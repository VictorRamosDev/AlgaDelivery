server:
  port: 8081

spring:
  application:
    name: Delivery-Tracking

  datasource:
    url: jdbc:mysql://localhost:3306/algadelivery?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: 12345

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect