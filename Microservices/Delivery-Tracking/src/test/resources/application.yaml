server:
  port: 8081

spring:
  application:
    name: Delivery-Tracking

  datasource:
    url: jdbc:mysql://localhost:3306/deliverydb_test?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
    username: root #root #(caso execute localmente)
    password: 12345

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect